#ddev-generated
[program:sshd]
command=/usr/local/bin/ai-ssh-setup.sh
autostart=true
autorestart=true
# /tmp instead of /var/log: since DDEV v1.25.3 /var/log is no longer
# world-writable and supervisord runs as the project user (EACCES → FATAL)
stdout_logfile=/tmp/sshd.log
stderr_logfile=/tmp/sshd.log
